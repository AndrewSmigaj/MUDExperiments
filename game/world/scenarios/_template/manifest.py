"""<scenario> manifest — PURE data (importable without Evennia, so `make validate` can lint it).

Copy this template to world/scenarios/<name>/manifest.py and fill the lists.
"""
from __future__ import annotations

NAME = "_template"
MATERIALS: list = []
OPERATIONS: list = []
OBJECTS: list = []
RESPONSES: dict = {}
