"""world.sim.systems.weather — the storm arc (§8). Pure. Built in roadmap P7.

light → steady → heavy → near-whiteout → night (−15 to −20 °C), degrading visibility/audibility/fire/
tracks/rescue on the running clock; 2-3 seeded timed beats. See systems/README.md.
"""
from __future__ import annotations
