#!/usr/bin/env python3
"""coverage — report op×material matrix completeness + fuzz-corpus stats (DR-18). Roadmap P2.

Coverage = the operation×material matrix is populated + property-tested, PLUS a ≥10k-attempt seeded
fuzz corpus with 0 unresolved / 0 conservation violations / rescue reachable. No silent truncation:
anything dropped (top-N, sampling) is reported, never hidden.
"""
import sys

print("coverage: not built yet (roadmap P2).")
print("  It will report op×material matrix completeness + the fuzz-corpus stats (and log anything "
      "dropped — no silent truncation).")
sys.exit(0)
