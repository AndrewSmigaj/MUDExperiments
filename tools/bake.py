#!/usr/bin/env python3
"""bake — compile authored scenario sources into the baked runtime data (DR-17). Roadmap P2.

Will: validate authored sources (content-lint + run the conservation ledger over each transform's
pre/post) then write the numeric/indexed form the deterministic runtime loads. LLM-assisted drafting
happens earlier and offline; bake only validates + writes data.
"""
import sys

print("bake: not built yet (roadmap P2).")
print("  It will validate authored sources (content-lint + the conservation ledger over each "
      "transform) then write the baked numeric/indexed runtime data.")
print("  See docs/scenarios/whiteout/roadmap.md (P2) and game/world/sim/validation/README.md.")
sys.exit(0)
